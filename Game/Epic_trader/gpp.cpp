#include <bits/stdc++.h>
#include <windows.h>
using namespace std;

int main(){
    string inp;
    int n;
    freopen("data.gpp", "r", stdin);
    cin >> n;
    for(int i = 0; i < n ; ++i){
        cin >> inp;
        
    }
}